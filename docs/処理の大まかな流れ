Smogon記事 → グラフ作成フロー

JSON読み込み
↓

ポケモン単位でループ処理
↓

文ブロック抽出（set / overview / commentsごと）
↓

候補エンティティ列挙（ポケモン名 / 技 / アイテム）
↓

意味分類 / エッジ生成（LLMで関係を分類しJSON化）
↓

文ブロックメタ情報付与（tier / set / block_type）
↓

信頼度算出（LLM出力の信頼度スコアやルールベース補正）
↓

グラフ構造化（ノード: ポケモン/技/アイテム、エッジ: 関係、属性: メタ情報）
↓

グラフ保存 / 可視化（NetworkX / Neo4jなど）